# Smoke / test execution site profile
schema_version = "site_profile_v2"
name = "smoke"
site_type = "local"
description = "Fast single-worker local execution profile for tests and smoke workflows"

[runner]
type = "subprocess"
max_workers = 2
worker_concurrency = 1

[harness]
timeout_seconds = 60
